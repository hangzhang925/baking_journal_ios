# Baking Journal iOS

Native SwiftUI iOS app for local-first baking recipe planning and step-by-step baking.

- Swift + SwiftUI
- iOS 17+
- Local persistence with `UserDefaults` + `Codable`
- No backend, login, or cloud sync in this first version
